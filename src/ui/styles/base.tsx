import { css } from "utils/css";

css`
  body {
    margin: 0;
    padding: 0;
    overflow: hidden;
    font-family: "Inter", sans-serif;
    color: var(--primary);
  }

  body,
  body * {
    box-sizing: border-box;
  }
`;
